{% extends 'base.html.twig' %}

{% block title %}Modifier — {{ evenement.titre }}{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Modifier : {{ evenement.titre }}</h1>
        <a href="{{ path('app_evenement_index') }}" class="btn btn-outline-secondary">← Retour</a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            {{ include('evenement/_form.html.twig', { button_label: 'Enregistrer les modifications' }) }}
        </div>
    </div>
{% endblock %}
