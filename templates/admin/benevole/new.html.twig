{% extends 'base.html.twig' %}

{% block title %}Nouveau bénévole{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Nouveau bénévole</h1>
        <a href="{{ path('app_admin_benevole_index') }}" class="btn btn-outline-secondary">← Retour</a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            {{ include('admin/benevole/_form.html.twig', { button_label: 'Créer le compte' }) }}
        </div>
    </div>
{% endblock %}
